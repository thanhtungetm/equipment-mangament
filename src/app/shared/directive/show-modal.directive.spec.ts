import { ShowModalDirective } from './show-modal.directive';

describe('ShowModalDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowModalDirective();
    expect(directive).toBeTruthy();
  });
});
